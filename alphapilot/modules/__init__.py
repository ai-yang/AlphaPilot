"""Pluggable feature modules orchestrating the four systems.

Built-in modules live here; third-party modules can be shipped as
separate pip packages and discovered via the ``alphapilot.modules``
entry point group (see :mod:`alphapilot.kernel.registry`).
"""
